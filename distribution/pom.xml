<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <parent>
    <groupId>consulo</groupId>
    <artifactId>arch.ide</artifactId>
    <version>2-SNAPSHOT</version>
    <relativePath>../arch/ide/pom.xml</relativePath>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>consulo-bundle</artifactId>
  <version>2-SNAPSHOT</version>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>2.2-beta-5</version>
        <executions>
          <execution>
            <id>create-archive-windows</id>
            <configuration>
              <descriptor>src/assembly/windows.xml</descriptor>
            </configuration>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
          <execution>
            <id>create-archive-linux</id>
            <configuration>
              <descriptor>src/assembly/linux.xml</descriptor>
            </configuration>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.google.code.maven-replacer-plugin</groupId>
        <artifactId>maven-replacer-plugin</artifactId>
        <version>1.4.0</version>
        <executions>
          <execution>
            <configuration>
              <file>platform/linux/bin/launcher.sh.template</file>
              <outputFile>platform/linux/bin/launcher.sh</outputFile>
              <replacements>
                <replacement>
                  <token>@project.version@</token>
                  <value>${project.version}</value>
                </replacement>
                <replacement>
                  <token>@jdom.version@</token>
                  <value>${jdom.version}</value>
                </replacement>
                <replacement>
                  <token>@log4j.version@</token>
                  <value>${log4j.version}</value>
                </replacement>
                <replacement>
                  <token>@trove4j.version@</token>
                  <value>${trove4j.version}</value>
                </replacement>
                <replacement>
                  <token>@jna.version@</token>
                  <value>${jna.version}</value>
                </replacement>
              </replacements>
            </configuration>
            <phase>process-sources</phase>
            <goals>
              <goal>replace</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>consulo</groupId>
      <artifactId>consulo-core-plugin</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>
</project>
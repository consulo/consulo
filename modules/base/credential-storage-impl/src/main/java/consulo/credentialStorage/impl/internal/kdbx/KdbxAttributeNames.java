package consulo.credentialStorage.impl.internal.kdbx;

/**
 * Provides attribute names for KDBX.
 */
public final class KdbxAttributeNames {
    // "protected" is a Java keyword, so we use PROTECTED_ATTR as the constant name.
    public static final String PROTECTED = "Protected";

    private KdbxAttributeNames() {
    }
}

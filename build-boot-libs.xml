<project name="boot-libs">
  <property name="out.dir" value="out-ant"/>
  <property name="lib.dir" value="out-ant/dist/lib"/>
  <property name="tmp.dir" value="out-ant/tmp"/>

  <path id="classpath">
    <fileset dir="lib">
      <include name="*.jar"/>
    </fileset>
  </path>

  <target name="build" depends="build-boot, build-bootstrap" />

  <target name="build-boot">
    <mkdir dir="${tmp.dir}/boot" />
    <javac classpathref="classpath" srcdir="platform/boot/src" destdir="${tmp.dir}/boot" />
    <jar basedir="${tmp.dir}/boot" destfile="${lib.dir}/boot.jar" />
  </target>

  <target name="build-bootstrap">
    <mkdir dir="${tmp.dir}/bootstrap" />
    <javac classpathref="classpath" srcdir="platform/bootstrap/src" destdir="${tmp.dir}/bootstrap" />
    <jar basedir="${tmp.dir}/bootstrap" destfile="${lib.dir}/bootstrap.jar" />
  </target>
</project>